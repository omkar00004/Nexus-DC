# --- LLM providers (multi-provider: Gemini for reasoning/vision, Groq for extraction) ---
google-genai>=1.0.0      # Gemini 3.1 Pro (high-stakes) / 3.5 Flash (high-volume) / Flash vision (P&ID)
groq>=0.11.0             # fast structured extraction (openai/gpt-oss-120b, fallback 20b)
# --- retrieval ---
chromadb>=0.5.0
sentence-transformers>=3.0.0
# --- document + schedule parsing (real formats) ---
pdfplumber>=0.11.0
PyMuPDF>=1.24.0
xerparser>=0.13.0        # Primavera P6 XER parser — DO NOT hand-roll one
openpyxl>=3.1.0          # read RFI register .xlsx
pytesseract>=0.3.10      # page-level OCR fallback for scanned spec/submittal pages
Pillow>=10.0.0
# --- graph / sim / data ---
networkx>=3.3
numpy>=2.0
pandas>=2.2
# --- api / infra ---
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
pydantic>=2.8.0
python-dotenv>=1.0.0
filelock>=3.15.0         # safe cross-process writes to events.json / kg.json
# --- upload handling (FastAPI multipart) ---
python-multipart>=0.0.9
# --- dev / test ---
pytest>=9.0              # tests/ - run with .venv/bin/pytest tests/
